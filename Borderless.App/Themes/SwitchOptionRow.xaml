<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:controls="clr-namespace:Borderless.App.Controls"
    xmlns:converters="clr-namespace:Borderless.App.Converters"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <BooleanToVisibilityConverter x:Key="SwitchOptionBoolToVis" />
    <converters:NullOrEmptyToVisibilityConverter x:Key="SwitchOptionEmptyToVis" />

    <Style TargetType="{x:Type controls:SwitchOptionRow}">
        <Setter Property="Margin" Value="0,0,0,12" />
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:SwitchOptionRow}">
                    <Border
                        Background="{TemplateBinding Background}"
                        Padding="0"
                        SnapsToDevicePixels="True"
                        x:Name="PART_Root">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <StackPanel
                                Grid.Column="0"
                                Margin="0,0,12,0"
                                VerticalAlignment="Center">
                                <ui:TextBlock
                                    Appearance="Primary"
                                    FontTypography="Body"
                                    Text="{TemplateBinding Title}"
                                    TextWrapping="Wrap" />
                                <ui:TextBlock
                                    Appearance="Tertiary"
                                    FontSize="10"
                                    Margin="0,2,0,0"
                                    Text="{TemplateBinding Description}"
                                    TextWrapping="Wrap"
                                    Visibility="{TemplateBinding Description,
                                                                 Converter={StaticResource SwitchOptionEmptyToVis}}" />
                            </StackPanel>

                            <Border
                                Background="Transparent"
                                Cursor="Help"
                                Grid.Column="1"
                                Height="28"
                                Margin="0,0,4,0"
                                VerticalAlignment="Center"
                                Visibility="{TemplateBinding InfoTooltipEnabled,
                                                             Converter={StaticResource SwitchOptionBoolToVis}}"
                                Width="28"
                                x:Name="PART_InfoHit">
                                <Border.ToolTip>
                                    <ToolTip Content="{TemplateBinding InfoTooltipContent}" MaxWidth="320" />
                                </Border.ToolTip>
                                <ui:SymbolIcon
                                    FontSize="14"
                                    Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                    HorizontalAlignment="Center"
                                    Symbol="Info16"
                                    VerticalAlignment="Center" />
                            </Border>

                            <ui:ToggleSwitch
                                Grid.Column="2"
                                IsChecked="{Binding IsChecked, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                IsEnabled="{TemplateBinding IsEnabled}"
                                VerticalAlignment="Center" />
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Cursor" Value="Arrow" />
                            <Setter Property="Opacity" Value="0.55" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Opacity" TargetName="PART_Root" Value="0.92" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
